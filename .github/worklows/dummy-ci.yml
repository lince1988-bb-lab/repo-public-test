name: Dummy CI

on:
  pull_request:
  push:
    branches:
      - main
      - develop

jobs:
  dummy-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run dummy test
        run: echo "Dummy CI executed successfully"
