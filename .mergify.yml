pull_request_rules:
  - name: Lab validation - merge PR when CI passes
    conditions:
      - base=main
      - check-success=dummy-test
      - label=mv-004-required-label
    actions:
      merge:
        method: merge
