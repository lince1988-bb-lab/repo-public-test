pull_request_rules:
  - name: MV-005 validation - invalid YAML is rejected safely
    conditions:
      - base=main
      - check-success=dummy-test
    actions:
      merge:
        method: merge
      invalid_yaml_break
