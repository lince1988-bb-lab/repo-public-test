pull_request_rules:
  - name: Lab validation - comment on PR
    conditions:
      - base=main
    actions:
      comment:
        message: |
          Mergify lab validation: rule matched successfully.
