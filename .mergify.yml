pull_request_rules:
  - name: MV-007 validation - unknown action is rejected safely
    conditions:
      - base=main
      - check-success=dummy-test
    actions:
      this_action_does_not_exist:
        enabled: true
